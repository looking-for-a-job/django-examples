#!/usr/bin/env python
from django.db import models
import django
django.setup()


class model(models.Model):
    field = models.CharField(max_length=30)
    last_name = models.CharField(max_length=30)


print(model.field)
print(model.field.__class__)
print(model.field.field_name)
