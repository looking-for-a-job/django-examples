#!/usr/bin/env python

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': 'dbname'
    }
}

DATABASES = {'default': {'ENGINE': 'django.db.backends.sqlite3','NAME': 'dbname'}}

